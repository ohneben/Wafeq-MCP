{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.ohneben/wafeq-mcp",
  "title": "Wafeq",
  "description": "Wafeq accounting API: all 251 endpoints as safety-categorized MCP tools, over stdio or HTTP.",
  "version": "2.0.0",
  "websiteUrl": "https://github.com/ohneben/Wafeq-MCP#readme",
  "repository": {
    "url": "https://github.com/ohneben/Wafeq-MCP",
    "source": "github",
    "id": "1356923464"
  },
  "packages": [
    {
      "registryType": "oci",
      "identifier": "ghcr.io/ohneben/wafeq-mcp:2.0.0",
      "runtimeHint": "docker",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "MCP_TRANSPORT",
          "description": "Server transport. Set to 'stdio' for local MCP clients; the image otherwise starts the Streamable HTTP transport.",
          "isRequired": true,
          "default": "stdio",
          "choices": [
            "stdio",
            "http"
          ]
        },
        {
          "name": "WAFEQ_API_KEY",
          "description": "Private organization API key, sent as `Authorization: Api-Key <key>`. Wafeq -> Settings -> Developer -> API keys. Required unless WAFEQ_ACCESS_TOKEN is set.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "WAFEQ_ACCESS_TOKEN",
          "description": "OAuth2 app access token, sent as `Authorization: Bearer <token>`. Use instead of WAFEQ_API_KEY.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "WAFEQ_TOOL_GROUPS",
          "description": "Comma-separated resource groups to expose, e.g. invoices,bills,reports. Empty exposes all 251 generated tools.",
          "isRequired": false
        },
        {
          "name": "WAFEQ_API_BASE_URL",
          "description": "Overrides the Wafeq API base URL. Defaults to https://api.wafeq.com/v1",
          "isRequired": false,
          "default": "https://api.wafeq.com/v1"
        },
        {
          "name": "MCP_SHARED_TOKEN",
          "description": "Bearer token required on the /mcp endpoint when running the HTTP transport. Empty leaves the endpoint open, so bind it to localhost only.",
          "isRequired": false,
          "isSecret": true
        }
      ]
    }
  ]
}
